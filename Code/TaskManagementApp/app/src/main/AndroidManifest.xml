<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions required for Notifications, Alarms, and Biometrics -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.INTERNET" />

    <!--
      Pomodoro Timer Foreground Service.
      - FOREGROUND_SERVICE: bắt buộc từ API 28.
      - FOREGROUND_SERVICE_SPECIAL_USE: bắt buộc từ API 34, mỗi foregroundServiceType
        cần permission riêng. Không dùng "shortService" vì loại đó bị hệ thống giới hạn
        khoảng 3 phút -> không đủ cho phiên tập trung 25 phút.
    -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <!--
      VIBRATE: cần cho việc rung khi phiên Pomodoro kết thúc — cả cho channel cảnh báo
      (vibrationPattern) lẫn đường dự phòng PomodoroAlertPlayer khi không đăng được notification.
    -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:name=".TaskApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TaskManagementApp">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>

        <activity
            android:name=".ui.detail.TaskDetailActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".ui.activity.AddEditTaskActivity"
            android:exported="false"
            android:label="@string/add_task"
            android:theme="@style/Theme.TaskManagementApp"
            android:windowSoftInputMode="adjustResize" />

        <activity
            android:name=".ui.activity.ImportActivity"
            android:exported="false"
            android:label="@string/import_tasks"
            android:theme="@style/Theme.TaskManagementApp" />

        <receiver
            android:name=".receiver.TaskNotificationReceiver"
            android:exported="false" />

        <receiver
            android:name=".receiver.AlarmReceiver"
            android:exported="false" />

        <receiver
            android:name=".receiver.StreakReminderReceiver"
            android:exported="false" />

        <receiver
            android:name=".receiver.TimeChangeReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.TIME_SET" />
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
                <action android:name="android.intent.action.DATE_CHANGED" />
            </intent-filter>
        </receiver>

        <!-- Khôi phục alarms sau khi thiết bị khởi động lại -->
        <receiver
            android:name=".receiver.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!-- PIN Lock Screen (TASK-25) -->
        <activity
            android:name=".ui.pin.PinLockActivity"
            android:exported="false"
            android:label="@string/app_name"
            android:windowSoftInputMode="stateAlwaysHidden" />

        <!-- Home Screen Widget (TMA-56) -->
        <receiver
            android:name=".widget.TaskWidgetProvider"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="android.appwidget.action.APPWIDGET_OPTIONS_CHANGED" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_today_tasks_info" />
        </receiver>

        <service
            android:name=".widget.TaskWidgetService"
            android:exported="false"
            android:permission="android.permission.BIND_REMOTEVIEWS" />

        <!--
          Pomodoro Timer (foreground).
          - started service (không bind) -> exported=false, không cần intent-filter.
          - foregroundServiceType="specialUse" (API 34+): đồng hồ tập trung do người dùng
            khởi tạo, phải chạy liên tục khi màn hình tắt / app ở background.
          - <property> khai báo lý do sử dụng cho Play Console (bắt buộc với specialUse).
        -->
        <service
            android:name=".pomodoro.PomodoroService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="pomodoro_focus_timer" />
        </service>

    </application>

</manifest>
