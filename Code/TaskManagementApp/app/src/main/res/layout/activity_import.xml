<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/background">

    <!-- Toolbar -->
    <com.google.android.material.appbarcompat.AppBarLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <androidx.appcompat.widget.Toolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="@color/primary"
            android:elevation="4dp"
            app:navigationIcon="@drawable/ic_arrow_back"
            app:title="Import Tasks" />
    </com.google.android.material.appbarcompat.AppBarLayout>

    <!-- Main Container -->
    <FrameLayout
        android:id="@+id/container_main"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:padding="24dp"
            android:gravity="center">

            <!-- Icon -->
            <ImageView
                android:layout_width="100dp"
                android:layout_height="100dp"
                android:src="@drawable/ic_upload"
                android:contentDescription="@null"
                android:tint="@color/primary"
                android:layout_marginBottom="24dp" />

            <!-- Title -->
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Import Tasks từ JSON"
                android:textSize="24sp"
                android:textStyle="bold"
                android:textColor="?attr/colorOnBackground"
                android:layout_marginBottom="8dp" />

            <!-- Description -->
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Chọn file JSON để import danh sách công việc"
                android:textSize="14sp"
                android:textColor="@color/outline"
                android:gravity="center"
                android:layout_marginBottom="32dp" />

            <!-- Selected File -->
            <TextView
                android:id="@+id/tv_selected_file"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Chưa chọn file"
                android:textSize="12sp"
                android:textColor="@color/outline"
                android:gravity="center"
                android:layout_marginBottom="32dp"
                android:padding="12dp"
                android:background="@color/surface_container_lowest"
                android:layout_marginStart="16dp"
                android:layout_marginEnd="16dp" />

            <!-- Import Button -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btn_import_file"
                android:layout_width="match_parent"
                android:layout_height="48dp"
                android:text="📁 Chọn File JSON"
                android:layout_marginStart="16dp"
                android:layout_marginEnd="16dp"
                android:layout_marginBottom="16dp"
                app:cornerRadius="8dp" />

            <!-- Conflict Resolution Buttons (hidden initially) -->
            <LinearLayout
                android:id="@+id/container_conflicts"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:visibility="gone"
                android:layout_marginStart="16dp"
                android:layout_marginEnd="16dp">

                <TextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Xử lý Task Trùng:"
                    android:textSize="14sp"
                    android:textStyle="bold"
                    android:textColor="@color/on_background"
                    android:layout_marginBottom="12dp" />

                <com.google.android.material.button.MaterialButton
                    android:id="@+id/btn_skip_conflicts"
                    android:layout_width="match_parent"
                    android:layout_height="44dp"
                    android:text="⏭️ Skip Conflicts"
                    android:layout_marginBottom="8dp"
                    app:cornerRadius="6dp" />

                <com.google.android.material.button.MaterialButton
                    android:id="@+id/btn_replace_all"
                    android:layout_width="match_parent"
                    android:layout_height="44dp"
                    android:text="♻️ Replace All"
                    app:cornerRadius="6dp" />
            </LinearLayout>

        </LinearLayout>
    </FrameLayout>

    <!-- Progress Bar (overlay) -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="gone">

        <ProgressBar
            android:id="@+id/progress_bar"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:indeterminate="true" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:gravity="center"
            android:background="#80000000">

            <ProgressBar
                android:layout_width="48dp"
                android:layout_height="48dp"
                android:indeterminate="true" />

            <TextView
                android:id="@+id/tv_status"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="⏳ Đang import..."
                android:textColor="@android:color/white"
                android:textSize="16sp"
                android:layout_marginTop="16dp" />
        </LinearLayout>
    </FrameLayout>

    <!-- Result Container -->
    <FrameLayout
        android:id="@+id/container_result"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="gone">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:padding="24dp"
            android:gravity="center">

            <!-- Result Title -->
            <TextView
                android:id="@+id/tv_result_title"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textSize="24sp"
                android:textStyle="bold"
                android:layout_marginBottom="16dp"
                tools:text="✅ Import Thành Công" />

            <!-- Result Message -->
            <TextView
                android:id="@+id/tv_result_message"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textSize="16sp"
                android:gravity="center"
                android:layout_marginBottom="32dp"
                tools:text="10 tasks imported\n5 tasks skipped" />

            <!-- Retry Button -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btn_retry"
                android:layout_width="match_parent"
                android:layout_height="48dp"
                android:text="🔄 Import Lại"
                android:layout_marginStart="16dp"
                android:layout_marginEnd="16dp"
                app:cornerRadius="8dp" />
        </LinearLayout>
    </FrameLayout>

</LinearLayout>
