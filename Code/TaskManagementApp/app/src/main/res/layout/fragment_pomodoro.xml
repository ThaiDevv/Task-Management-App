<?xml version="1.0" encoding="utf-8"?>
<!--
  Layout: fragment_pomodoro.xml
  TASK 9 — Pomodoro Timer Screen.
  Bố cục: Session type → Countdown + Circular progress → Cycle dots → Controls.
  Màn hình này KHÔNG đếm giờ: mọi giá trị đều được Fragment đẩy vào từ state của Service.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background"
    android:orientation="vertical"
    tools:context=".ui.pomodoro.PomodoroFragment">

    <!-- ─── Top App Bar ─── -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingStart="4dp"
        android:paddingEnd="16dp">

        <ImageButton
            android:id="@+id/backButton"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/action_back"
            android:padding="12dp"
            android:src="@drawable/ic_back"
            app:tint="@color/on_surface" />

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:fontFamily="@font/poppins_semibold"
            android:text="@string/pomodoro_title"
            android:textColor="@color/on_surface"
            android:textSize="18sp" />

    </LinearLayout>

    <androidx.core.widget.NestedScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:fillViewport="true"
        android:paddingBottom="24dp">

        <LinearLayout
            android:id="@+id/pomodoroBody"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical"
            android:gravity="center_horizontal"
            android:orientation="vertical"
            android:paddingStart="24dp"
            android:paddingEnd="24dp">

            <!-- ─── 1. Session type ─── -->
            <TextView
                android:id="@+id/tvSessionType"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:fontFamily="@font/poppins_semibold"
                android:letterSpacing="0.12"
                android:text="@string/pomodoro_session_focus"
                android:textAllCaps="true"
                android:textColor="@color/primary"
                android:textSize="14sp" />

            <TextView
                android:id="@+id/tvCycleLabel"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:fontFamily="@font/poppins_medium"
                android:textColor="@color/on_surface_variant"
                android:textSize="13sp"
                tools:text="Chu kỳ 1/4" />

            <!-- ─── 2. Countdown + Circular progress ─── -->
            <FrameLayout
                android:layout_width="260dp"
                android:layout_height="260dp"
                android:layout_marginTop="24dp">

                <com.team.taskmanagementapp.ui.custom.PomodoroProgressRingView
                    android:id="@+id/viewProgressRing"
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:contentDescription="@string/pomodoro_progress_description" />

                <LinearLayout
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_gravity="center"
                    android:gravity="center"
                    android:orientation="vertical">

                    <TextView
                        android:id="@+id/tvCountdown"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:fontFamily="@font/poppins_bold"
                        android:letterSpacing="-0.02"
                        android:textColor="@color/on_surface"
                        android:textSize="56sp"
                        tools:text="25:00" />

                    <TextView
                        android:id="@+id/tvTimerState"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:fontFamily="@font/poppins_medium"
                        android:textColor="@color/on_surface_variant"
                        android:textSize="14sp"
                        tools:text="Sẵn sàng" />

                </LinearLayout>

            </FrameLayout>

            <!-- ─── 3. Cycle dots (placeholder — chưa có cycle selector) ─── -->
            <LinearLayout
                android:id="@+id/cycleDotsContainer"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="24dp"
                android:gravity="center"
                android:orientation="horizontal" />

            <TextView
                android:id="@+id/tvFocusSummary"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="10dp"
                android:fontFamily="@font/poppins_regular"
                android:textColor="@color/on_surface_variant"
                android:textSize="13sp"
                tools:text="Đã hoàn thành 2 phiên tập trung" />

            <!-- ─── 4. Controls ─── -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnPrimary"
                android:layout_width="match_parent"
                android:layout_height="56dp"
                android:layout_marginTop="28dp"
                android:fontFamily="@font/poppins_semibold"
                android:text="@string/pomodoro_action_start"
                android:textAllCaps="false"
                android:textColor="@color/on_primary"
                android:textSize="16sp"
                app:backgroundTint="@color/primary"
                app:cornerRadius="18dp"
                app:icon="@drawable/ic_play"
                app:iconGravity="textStart"
                app:iconTint="@color/on_primary" />

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="12dp"
                android:orientation="horizontal">

                <com.google.android.material.button.MaterialButton
                    android:id="@+id/btnPause"
                    android:layout_width="0dp"
                    android:layout_height="52dp"
                    android:layout_weight="1"
                    android:fontFamily="@font/poppins_medium"
                    android:text="@string/pomodoro_action_pause"
                    android:textAllCaps="false"
                    android:textColor="@color/on_surface"
                    android:textSize="14sp"
                    app:backgroundTint="@color/surface_container_lowest"
                    app:cornerRadius="16dp"
                    app:strokeColor="@color/outline_variant"
                    app:strokeWidth="1dp" />

                <com.google.android.material.button.MaterialButton
                    android:id="@+id/btnSkip"
                    android:layout_width="0dp"
                    android:layout_height="52dp"
                    android:layout_marginStart="10dp"
                    android:layout_weight="1"
                    android:fontFamily="@font/poppins_medium"
                    android:text="@string/pomodoro_action_skip"
                    android:textAllCaps="false"
                    android:textColor="@color/on_surface"
                    android:textSize="14sp"
                    app:backgroundTint="@color/surface_container_lowest"
                    app:cornerRadius="16dp"
                    app:strokeColor="@color/outline_variant"
                    app:strokeWidth="1dp" />

                <com.google.android.material.button.MaterialButton
                    android:id="@+id/btnStop"
                    android:layout_width="0dp"
                    android:layout_height="52dp"
                    android:layout_marginStart="10dp"
                    android:layout_weight="1"
                    android:fontFamily="@font/poppins_medium"
                    android:text="@string/pomodoro_action_stop"
                    android:textAllCaps="false"
                    android:textColor="@color/tertiary"
                    android:textSize="14sp"
                    app:backgroundTint="@color/surface_container_lowest"
                    app:cornerRadius="16dp"
                    app:strokeColor="@color/outline_variant"
                    app:strokeWidth="1dp" />

            </LinearLayout>

        </LinearLayout>

    </androidx.core.widget.NestedScrollView>

</LinearLayout>
