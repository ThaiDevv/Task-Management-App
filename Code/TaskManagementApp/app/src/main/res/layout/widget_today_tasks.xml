<?xml version="1.0" encoding="utf-8"?>
<!-- Widget "Việc hôm nay" — bento card hiện đại: bo góc lớn, header tinted,
     progress bar mảnh, danh sách task với priority stripe bên trái từng dòng. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/widget_background">

    <!-- Header: chạm để mở app -->
    <LinearLayout
        android:id="@+id/widget_header"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingStart="16dp"
        android:paddingEnd="12dp"
        android:paddingTop="14dp"
        android:paddingBottom="10dp"
        android:background="@drawable/widget_header_background">

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">

            <TextView
                android:id="@+id/widget_title_text"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="@string/widget_today_title"
                android:fontFamily="@font/poppins_semibold"
                android:textSize="16sp"
                android:textColor="@color/widget_on_surface"
                android:maxLines="1"
                android:ellipsize="end" />

            <TextView
                android:id="@+id/widget_date_text"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:fontFamily="@font/poppins_regular"
                android:textSize="11sp"
                android:textColor="@color/widget_on_surface_variant"
                android:maxLines="1" />
        </LinearLayout>

        <TextView
            android:id="@+id/widget_count_chip"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/widget_chip_background"
            android:paddingStart="10dp"
            android:paddingEnd="10dp"
            android:paddingTop="3dp"
            android:paddingBottom="3dp"
            android:fontFamily="@font/poppins_medium"
            android:textSize="11sp"
            android:textColor="@color/widget_on_chip"
            android:maxLines="1" />

        <!-- Nút thêm việc mới -->
        <ImageView
            android:id="@+id/widget_add_button"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:layout_marginStart="8dp"
            android:background="@drawable/widget_add_button_background"
            android:src="@drawable/widget_ic_add"
            android:contentDescription="@string/widget_add_task"
            android:scaleType="center" />
    </LinearLayout>

    <!-- Thanh tiến độ ngày -->
    <ProgressBar
        android:id="@+id/widget_progress"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="match_parent"
        android:layout_height="4dp"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:layout_marginTop="2dp"
        android:progressDrawable="@drawable/widget_progress_drawable"
        android:max="100" />

    <!-- Danh sách task của ngày -->
    <ListView
        android:id="@android:id/list"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:divider="@null"
        android:dividerHeight="0dp"
        android:paddingTop="4dp"
        android:paddingBottom="6dp"
        android:scrollbars="none" />

    <!-- Trạng thái trống -->
    <TextView
        android:id="@+id/widget_empty_view"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:gravity="center"
        android:padding="16dp"
        android:text="@string/widget_empty_message"
        android:fontFamily="@font/poppins_regular"
        android:textSize="13sp"
        android:lineSpacingExtra="4dp"
        android:textColor="@color/widget_on_surface_variant"
        android:visibility="gone" />
</LinearLayout>
